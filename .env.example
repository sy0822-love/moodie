PORT=4175

# Optional: server-side Gemini proxy
GEMINI_API_KEY=

# Optional: use Supabase instead of the built-in file DB
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_TABLE=diary_entries

# Optional: where the built-in file DB is stored
DATA_DIR=./data
